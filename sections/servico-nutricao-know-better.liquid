{% assign path_block = section.blocks | where: 'type', 'path' %}

<style>
  #servico_know_custom {
    height: 1310px;
  }

  #servico_know_custom > div {
    display: grid;
    grid-auto-rows: max-content;
    grid-auto-flow: row;
    gap: 1rem;
    border-radius: 1rem;
    background-color: white;
    padding: 2rem 1rem;
  }

  #servico_know_custom > div > h1  {
    margin: 0;
    padding: 0;
    font-size: 3rem;
    font-weight: 900 !important;
    line-height: normal;
  }

  #servico_know_custom > div > p {
    margin: 0;
    padding: 0;
    font-size: 1.25rem;
    font-weight: 500;
    line-height: normal;
  }

  #servico_know_custom > ul {
    display: grid;
    gap: 1rem;
    grid-auto-columns: max-content;
    grid-auto-flow: column;
    list-style-type: none;
  }

  #servico_know_custom > ul > li {
    padding: 2rem 1rem;
    background-color: #258439;
    color: white;
    border-radius: 1rem;
    margin: 0;
    display: grid;
    grid-auto-rows: max-content;
    grid-auto-flow: row;
    gap: 1rem;
    justify-items: center;
    max-width: 549px;
  }

  #servico_know_custom > ul > li > h1 {
    margin: 0;
    padding: 0;
    font-size: 1.5rem;
    font-weight: 900 !important;
    line-height: normal;
  }

  #servico_know_custom > ul > li > p {
    margin: 0;
    padding: 0;
    font-size: 1rem;
    font-weight: 500 !important;
    line-height: normal;
  }
</style>

<section
  id="servico_know_custom"
  class="lazy-bg"
  data-bg="{{ section.settings.bg_img | image_url: width: '1923', height: '1310' }}"
  style="background-size: cover; background-position: center;"
>
  {% unless section.settings.title == blank %}
    <div>
      <h1>{{ section.settings.title }}</h1>

      {% if section.settings.description != blank %}
        {{ section.settings.description }}
      {% endif %}
    </div>
  {% endunless %}

  {% unless path_block.size == 0 %}
    <ul>
      {% for p in path_block %}
        <li>
          {% unless p.settings.icon %}
            <img
              src="{{ p.settings.icon | image_url: width: 80, height: 80 }}"
              alt="{{ forloop.index }}-icon"
              width="80"
              height="80"
            >
          {% endunless %}

          {% unless p.settings.title == blank %}
            <h1>{{ p.settings.title }}</h1>
          {% endunless %}

          {% unless p.settings.description == blank %}
            {{ p.settings.description }}
          {% endunless %}
        </li>
      {% endfor %}
    </ul>
  {% endunless %}
</section>

{% schema %}
{
  "name": "Servico Nutricao Know",
  "settings": [
    {
      "type": "image_picker",
      "id": "bg_img",
      "label": "Imagem de Fundo"
    },
    {
      "type": "richtext",
      "id": "title",
      "label": "Title"
    },
    {
      "type": "richtext",
      "id": "description",
      "label": "Descrição"
    }
  ],
  "blocks": [
    {
      "type": "path",
      "name": "Percurso",
      "limit": 3,
      "settings": [
        {
          "type": "image_picker",
          "id": "icon",
          "label": "Ícone"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Título"
        },
        {
          "type": "richtext",
          "id": "description",
          "label": "Descrição"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Servico Nutricao Know",
      "category": "custom"
    }
  ]
}
{% endschema %}
